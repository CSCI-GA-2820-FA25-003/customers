apiVersion: v1
kind: Secret
metadata:
  name: pg-credentials
type: Opaque
stringData:
  username: postgres
  password: postgres
  dbname: customers
  # 方便微服务直接引用的连接串（可选）
  url: postgres://postgres:postgres@postgres:5432/customers
